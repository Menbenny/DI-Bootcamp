{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Open with Chrome",
            "type": "shell",
            "command": "start chrome ${file}",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
